fun main() {
    // Цикл for, который итерирует по числам от 1 до 20
    for (i in 1..20) {
        // Проверка, является ли число четным
        if (i % 2 == 0) {
            // Вывод четного числа на консоль
            println(i)
        }
    }
}